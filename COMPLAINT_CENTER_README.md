# ระบบจัดการเรื่องร้องเรียน (Complaint Center - Global Mode)

เอกสารนี้สรุปการทำงานของระบบร้องทุกข์ร้องเรียนที่ได้รับการปรับปรุงใหม่ โดยใช้ระบบ **Global Mode Toggle** เพื่อควบคุมรูปแบบการทำงาน (Workflow) ของทั้งเทศบาลผ่านจุดเดียว

---

## 🚦 สรุปสถานะเรื่องร้องเรียน (Status Flow)

| Status (DB/API) | คำแปล Admin (Thai) | คำแปล Mobile (Thai) | ความหมาย |
| :---            | :---               | :---                | :--- |
| **`draft`** | - | **แบบร่าง**         | เรื่องที่ยังเขียนไม่เสร็จ (Admin มองไม่เห็น) |
| **`pending`** | **รับเรื่อง** | **รับเรื่อง** | เรื่องส่งเข้ามาแล้ว รอการจัดการ (จ่ายงาน หรือ รับเรื่องเอง) |
| **`received`** | **รับเรื่อง** | **รับเรื่อง** | กองได้รับมอบหมายงานแล้ว หรือกดรับเรื่องแล้ว |
| **`in_progress`** | **กำลังดำเนินการ** | **ดำเนินการ** | อยู่ระหว่างการแก้ไขปัญหาโดยเจ้าหน้าที่ |
| **`completed`** | **เสร็จสิ้น** | **เสร็จสิ้น** | แก้ไขปัญหาเรียบร้อยแล้ว |
| **`rejected`** | **ตีกลับศูนย์** | **รับเรื่อง** | กองพิจารณาแล้วทำไม่ได้ จึงตีกลับไปให้ศูนย์ฯ พิจารณาใหม่ |

---

## ⚙️ 1. ระบบควบคุมโหมด (Global Mode Toggle)
ระบบทำงานโดยอ้างอิงค่าจากฟิลด์ **`complaint_mode`** ในตาราง `municipalities` (ข้อมูลเทศบาล) ซึ่ง Super Admin สามารถสลับโหมดได้จากหน้าบ้าน:

1.  **`direct` (Option 1 - แบบส่งตรงเข้ากอง)**:
    *   **กติกา**: เรื่องจะวิ่งไปที่หน้า "จัดการเรื่องร้องเรียน" ของกองที่รับผิดชอบหมวดหมู่นั้นๆ ทันที
    *   **หน้าศูนย์ฯ**: จะถูกปิดการทำงาน (ไม่มีข้อมูลแสดง) เพราะไม่มีขั้นตอนการ Triage

2.  **`central` (Option 2 - แบบผ่านศูนย์กลาง)**:
    *   **กติกา**: เรื่องใหม่ทั้งหมดจะไปรวมอยู่ที่ "ศูนย์ร้องทุกข์" เพื่อให้แอดมินกลาง (ปลัด) เป็นคนจ่ายงาน
    *   **หน้าจัดการปกติ**: แอดมินกองงานจะมองไม่เห็นเรื่องใหม่จนกว่าศูนย์ฯ จะกดมอบหมายงานมาให้

---

## 🔄 2. สรุป Workflow การทำงาน

### [Mode: Direct] เน้นรวดเร็ว
1.  **ประชาชน**: ส่งเรื่องร้องเรียน -> ระบบตั้งค่า `status = 'pending'`, `department_id = NULL`
2.  **แอดมินกอง**: เรื่องจะโผล่ในหน้าจัดการทันทีหาก "หมวดหมู่ตรงกับที่กองดูแล"
3.  **การทำงาน**: กองกดรับเรื่อง (`received`) และดำเนินการจนเสร็จสิ้น (`completed`)

### [Mode: Central] เน้นการควบคุม
1.  **ประชาชน**: ส่งเรื่องร้องเรียน -> ระบบตั้งค่า `status = 'pending'`, `department_id = NULL`
2.  **แอดมินศูนย์ฯ**: เห็นเรื่องในหน้า Dashboard ศูนย์ฯ -> กด "มอบหมายงาน" ให้กองที่เกี่ยวข้อง
3.  **ระบบ**: อัปเดต `department_id = กองที่เลือก` และ `status = 'received'`
4.  **แอดมินกอง**: เรื่องจะไปโผล่ที่หน้าจัดการของกองนั้นๆ เพื่อดำเนินการต่อ

---

## 👁️ 3. สิทธิ์การมองเห็น (Visibility Logic)

หลังบ้าน (Backend) จะคัดกรองข้อมูลโดยเช็คจาก `complaint_mode` ดังนี้:

*   **ถ้าโหมดเป็น `central`**:
    *   **แอดมินศูนย์ฯ**: ในหน้า Dashboard ศูนย์ฯ เห็นเรื่องใหม่ทั้งหมด (ที่ยังไม่มีเจ้าภาพ)
    *   **แอดมินทุกคน**: ในหน้าจัดการปกติ เห็นเฉพาะเรื่องที่มีการมอบหมาย (`department_id`) ตรงกับกองของตนเองเท่านั้น
*   **ถ้าโหมดเป็น `direct`**:
    *   **แอดมินทุกคน**: ในหน้าจัดการปกติ เห็นเรื่องที่จ่ายมาที่กองตนเอง **หรือ** เรื่องใหม่ที่ยังไม่มีเจ้าภาพแต่หมวดหมู่ตรงกับสิทธิ์ที่ตนเองมี

---

## 🛠 4. รายละเอียดทางเทคนิค (Technical Note)

### **Backend (Go)**
- **Domain**: เพิ่มฟิลด์ `ComplaintMode` ใน `Municipality` struct
- **Repository**: ใช้ `isCentralMode` ใน `GetPaginated` เพื่อแยก Branch การ Query ข้อมูลอย่างชัดเจน
- **JWT**: มีการฉีดสิทธิ์ `ModuleComplaintCenter` ให้แอดมินที่มีสิทธิ์อัตโนมัติ

### **Frontend (Admin)**
- **UI Settings**: เพิ่มวิทยุเลือกโหมดในหน้า **Super Admin > ข้อมูลเทศบาล**
- **Proxy**: ปรับปรุง Middleware ใน `proxy.ts` ให้คุมเส้นทาง `/complaint-center` และ `/complaint` แยกกันเด็ดขาด

### **Mobile App**
- **Status Sync**: ปรับปรุง `StatusBadge` และ Workflow ให้เหลือ 3 สถานะหลัก เพื่อความง่ายและสอดคล้องกับระบบแอดมิน
