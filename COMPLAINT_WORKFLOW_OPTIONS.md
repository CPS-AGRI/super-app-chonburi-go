# รายละเอียดทางเทคนิค: ระบบจัดการเรื่องร้องเรียน (Workflow Options)

เอกสารฉบับนี้อธิบายถึงการทำงานและ Logic ทางเทคนิคของระบบจัดการเรื่องร้องเรียนที่มี 2 รูปแบบ (Workflow Modes)

## การตั้งค่า (Configuration)
พฤติกรรมของระบบจะถูกควบคุมโดยการตั้งค่าในระดับเทศบาล (Municipality)

- **ตาราง (Table)**: `municipalities`
- **ฟิลด์ (Field)**: `complaint_mode` (ประเภท: `text`)
- **ค่าที่เป็นไปได้ (Values)**: 
    - `'direct'`: Option 1 (แบบส่งตรงเข้ากอง)
    - `'central'`: Option 2 (แบบผ่านศูนย์กลาง) - *ค่าเริ่มต้น*

---

## Option 1: แบบส่งตรงเข้ากอง (Direct to Department)

### ภาพรวมการทำงาน
เรื่องร้องเรียนที่ส่งมาจากประชาชนจะถูก **กรองและแสดงที่กองที่รับผิดชอบโดยอัตโนมัติ** ทันทีที่สร้างเรื่อง โดยอ้างอิงจากหมวดหมู่ (**ModuleType**) ที่เลือก

> **ศูนย์ร้องทุกข์ (Center) มองเห็นได้ทุกเรื่อง ทุกสถานะ และสามารถจัดการได้เช่นกัน** แต่ไม่ใช่ผู้คัดกรองก่อนส่งกอง — งานไปที่กองโดยตรงผ่าน module_type

### Logic ทางเทคนิค (Backend)
1. **การสร้างเรื่อง (`CreateComplaint` ใน UseCase)**:
    - **สถานะ (Status)**: จะถูกตั้งเป็น `pending` (รอดำเนินการ) ตามปกติ
    - **DepartmentId**: จะมีค่าเป็น `NULL` (กองรับงานผ่าน module_type filter)
2. **การมองเห็นและการคัดกรอง**:
    - **แอดมินประจำกอง**: เห็นเรื่องผ่าน `module_type_id` ที่กองนั้นรับผิดชอบ
    - **ศูนย์ร้องทุกข์ (Center)**: เห็น **ทุกเรื่อง ทุกสถานะ** (ไม่มีการกรอง) — สามารถ Forward/จัดการได้

### การเชื่อมโยงฐานข้อมูล (Database Mapping)
การหากองที่รับผิดชอบจากหมวดหมู่:
`module_types` -> `department_module_module_types` -> `department_modules` -> `departments`

---

## Option 2: แบบผ่านศูนย์กลาง (Central Triage)

### ภาพรวมการทำงาน
เรื่องร้องเรียน **จะไม่ไปที่กองโดยตรง** — ทุกเรื่องจะส่งเข้า "ศูนย์ร้องทุกข์" ก่อน จากนั้นแอดมินศูนย์กลางจะตรวจสอบและ "ส่งต่อ" (Forward) งานให้กองที่เกี่ยวข้องด้วยตัวเอง

### Logic ทางเทคนิค (Backend)
1. **การสร้างเรื่อง (`CreateComplaint` ใน UseCase)**:
    - **สถานะ (Status)**: จะถูกตั้งเป็น `pending` (รอดำเนินการ)
    - **DepartmentId**: จะมีค่าเป็น `NULL` (ยังไม่มีกองรับผิดชอบ)
2. **ขั้นตอนการคัดกรอง (Triage)**:
    - แอดมินศูนย์กลางจะเห็นเรื่องเหล่านี้ในรายการ "เรื่องที่ยังไม่ได้มอบหมาย"
    - แอดมินใช้ API `POST /api/v1/complaints/:id/forward` เพื่อส่งงานให้กองที่เกี่ยวข้อง
    - เมื่อส่งต่อแล้ว สถานะจะเปลี่ยนเป็น `received` (รับเรื่องแล้ว)

---

## รายละเอียด API

### 1. การส่งเรื่องร้องเรียน (Submit Complaint)
- **Endpoint**: `POST /api/v1/complaints`
- **Logic**: Backend จะเช็ค `complaint_mode` และจัดการเส้นทางของเรื่อง (Routing) และสถานะให้โดยอัตโนมัติ

### 2. การตีกลับเรื่อง (Reject/Return Complaint)
- **Endpoint**: `POST /api/v1/complaints/:id/reject`
- **Option 1 (Direct Mode)**: เมื่อกดตีกลับ ระบบจะเปลี่ยนสถานะกลับเป็น `received` (รับเรื่องแล้ว) ทันที และเรื่องยังคงอยู่ที่เดิมภายในกอง เพื่อให้แอดมินคนอื่นหรือหัวหน้ากองสามารถตรวจสอบและมอบหมายงานใหม่ได้ (Reset Flow ภายในกอง)
- **Option 2 (Central Mode)**: เมื่อกดตีกลับ สถานะจะเปลี่ยนเป็น `rejected` (ตีกลับศูนย์) และระบบจะล้างค่า `department_id` ทิ้ง เพื่อส่งเรื่องกลับไปที่รายการ "งานส่งกลับ" ในหน้าศูนย์ร้องทุกข์เพื่อให้แอดมินกลางพิจารณาใหม่

### 3. การมองเห็นของศูนย์ร้องทุกข์ (Center Visibility)
| | Option 1 (Direct) | Option 2 (Central) |
|---|---|---|
| เรื่องที่แสดงในศูนย์ | **ทุกเรื่อง ทุกสถานะ** | เรื่องที่ยังไม่ถูก Forward + งานส่งกลับ |
| Center จัดการได้ไหม | ได้ (สามารถ Forward ได้) | ได้ (คัดกรองก่อน Forward) |
| กองเห็นเรื่องเมื่อ | สร้างเรื่อง (ผ่าน module_type) | Center กด Forward เท่านั้น |
| แท็บในหน้าศูนย์ | รอ/รับเรื่อง / กำลังดำเนินการ / ประวัติ | เรื่องใหม่ / งานส่งกลับ / ประวัติมอบหมาย |

- **หน้าจัดการเรื่องร้องเรียน (`complaint/manage`)**:
    - **Option 2 (Central)**: ระบบจะ **ซ่อน** เรื่องที่มีสถานะ `rejected` ออกจากรายการของทุกกองงานโดยอัตโนมัติ
    - **Option 1 (Direct)**: กรองข้อมูลตาม `module_type_id` ที่กองนั้นๆ ได้รับมอบหมายสิทธิ์

---

## การสื่อสารผ่าน API (Frontend & Backend)

### 1. ความสม่ำเสมอของ API Path (API Consistency)
ทั้ง 2 โหมดการทำงานจะใช้งาน **API Endpoint เส้นเดียวกันทั้งหมด** เพื่อความสะดวกในการพัฒนา แต่ Backend จะจัดการ Logic ภายในให้ต่างกันตามโหมดที่ตั้งค่าไว้:
- **Frontend**: เรียกใช้ Path เดิมเสมอ (เช่น `GET /api/v1/complaints`)
- **Backend**: ตรวจสอบ `complaint_mode` จากฐานข้อมูล และปรับการ Query หรือ Update ข้อมูลให้สอดคล้องกับโหมดนั้นๆ โดยอัตโนมัติ

### 2. การตรวจสอบโหมดจากฝั่ง Frontend
เพื่อปรับปรุง UI (เช่น ซ่อน/แสดงปุ่ม) ฝั่ง Frontend จะดึงข้อมูลโหมดการทำงานผ่าน API ต่อไปนี้:
- **Endpoint**: `GET /api/municipality/public`
- **ข้อมูลที่ได้รับ**: ออบเจกต์เทศบาลที่มีฟิลด์ `complaintMode` (`'direct'` หรือ `'central'`)
- **การนำไปใช้**: ใช้ในการตัดสินใจเลือกแสดง UI Elements เช่น ปุ่ม "ตีกลับศูนย์" จะถูกซ่อนหากโหมดเป็น `direct`

### 3. การทดสอบด้วย API Client (Bruno / Postman)
ในการทดสอบรายการเรื่องร้องเรียนผ่าน API เส้น `GET /api/v1/complaints` สามารถใช้ Query Parameters เพื่อจำลองการดูข้อมูลในแต่ละแท็บของ Dashboard ได้ดังนี้:
- **เรื่องใหม่ (Unassigned)**: `?has_been_assigned=false`
- **งานส่งกลับ (Returned)**: `?status=rejected`
- **ประวัติการมอบหมาย (Assigned)**: `?has_been_assigned=true`

*หมายเหตุ: ผลลัพธ์ที่ได้จะถูกคำนวณจาก (1) สิทธิ์ของ Token ที่ใช้เรียก (2) ค่า `complaint_mode` ในฐานข้อมูล และ (3) Query Parameters ที่ส่งไปพร้อมกัน*

---

## ตารางฐานข้อมูลที่เกี่ยวข้อง

| ชื่อตาราง | หน้าที่ |
| :--- | :--- |
| `municipalities` | เก็บการตั้งค่า `complaint_mode` |
| `module_complaints` | เก็บข้อมูลหลักของคำร้อง, `status`, และ `department_id` |
| `department_modules` | เชื่อมโยง กอง กับ มอดูล (เช่น "ร้องทุกข์") |
| `department_module_module_types` | เชื่อมโยง กอง กับ หมวดหมู่ย่อย (เช่น "ขยะ", "ไฟฟ้า") |

---

## ตำแหน่งไฟล์โค้ด (Backend)

การทำงานของระบบร้องทุกข์ถูกแยกออกเป็น 2 Repository หลักตามประเภทการใช้งาน:

### 1. ระบบจัดการหลังบ้านแอดมิน (Admin Backend - `super-app-chonburi-go`)
- **นิยาม Interface**: `internal/domain/complaint.go`
- **Business Logic**: `internal/usecase/complaint_usecase.go` (ฟังก์ชัน: `CreateComplaint`, `GetComplaints`, `RejectComplaint`, `RateComplaint`)
- **Query Logic**: `internal/repository/complaint_repository.go` (ฟังก์ชัน: `GetAllowedModuleTypeIDs`, `GetPaginated`, `GetCompleterInfo` - *ระบบจัดสรรคะแนนแบบ Hybrid*)
- **การระบุแอดมินศูนย์กลาง**: ใช้การตรวจสอบชื่อมอดูล (Flexible Name Matching) เช่น "ศูนย์ร้องทุกข์", "ศูนย์รับเรื่อง" เพื่อระบุสิทธิ์แอดมินกลางโดยอัตโนมัติ

### 2. ระบบโมบายล์แอปสำหรับประชาชน (Mobile Backend - `super-app-chonburi-go-mobile`)
- **Business Logic**: `internal/usecase/complaint_usecase.go` (ฟังก์ชัน: `RateComplaint`)
- **Query Logic**: `internal/repository/complaint_repository.go` (ฟังก์ชัน: `GetCompleterInfo` - *ระบบจัดสรรคะแนนแบบ Hybrid*)

---

## ข้อควรระวังและรายละเอียดเพิ่มเติม

### 1. การสลับโหมดการทำงาน (Switching Modes)
- **จาก Option 2 -> Option 1**: เรื่องที่เคยมีสถานะ `rejected` (ตีกลับศูนย์) จะยังคงถูกซ่อนจากหน้า "จัดการเรื่องร้องเรียน" เพื่อไม่ให้เจ้าหน้าที่กองงานสับสนกับงานที่ไม่ได้อยู่ในความรับผิดชอบโดยตรง
- **จาก Option 1 -> Option 2**: เรื่องที่มีอยู่แล้วจะยังคงทำงานตามเดิม แต่เรื่องใหม่ที่เกิดขึ้นหลังจากสลับโหมดจะเข้าสู่กระบวนการคัดกรองของศูนย์กลาง

### 2. การระบุตัวตนแอดมินศูนย์กลาง (Center Admin Detection)
ระบบใช้การตรวจสอบชื่อมอดูลแบบ **Flexible Name Matching** (ไม่สนตัวเล็ก-ใหญ่) เพื่อระบุสิทธิ์แอดมินกลางโดยอัตโนมัติ หากต้องการสร้างมอดูลใหม่ให้แอดมินกลาง ควรมีคำเหล่านี้อยู่ในชื่อมอดูล:
- **ภาษาไทย**: "ศูนย์ร้องทุกข์", "ศูนย์รับเรื่อง"
- **ภาษาอังกฤษ**: "complaint center"

### 3. สรุปการมองเห็นและการใช้งาน (Visibility Matrix)

| ฟีเจอร์ / หน้าจอ | Option 1 (Direct) | Option 2 (Central) |
| :--- | :--- | :--- |
| **หน้าศูนย์กลาง (Dashboard)** | แสดงทุกเรื่อง ทุกสถานะ | แสดงเรื่องใหม่, งานส่งกลับ, และประวัติ |
| **หน้าจัดการกองงาน (Manage)** | แสดงงานตามหมวดหมู่ (Module Types) | แสดงงานตามหน่วยงาน (Departments) |
| **สถานะ Rejected ในหน้า Manage** | **ซ่อน** (เพื่อความสม่ำเสมอ) | **ซ่อน** (เพื่อไม่ให้กองงานสับสน) |
| **ปุ่ม "ตีกลับ"** | แสดงในหน้า complaint-center เท่านั้น | แสดงในหน้า complaint-center เท่านั้น |

---

## การจัดการสิทธิ์และการกรองข้อมูล (แก้ไขบั๊กและรองรับ Hybrid Mode)
ระบบได้ปรับปรุงการเช็คสิทธิ์แบบ **Strict Mapping Check** และเพิ่ม **Hybrid Visibility** เพื่อรองรับการสลับโหมดไปมาโดยข้อมูลไม่หาย:

### 1. Hybrid Visibility Logic (ใน Option 2)
เพื่อให้เจ้าหน้าที่กองงานเห็นประวัติงานเก่าที่ทำเสร็จแล้วจากโหมด Direct (Option 1) ซึ่งไม่มีการบันทึกค่า `department_id` ระบบจะใช้ Logic ดังนี้ในหน้าจัดการ:

- **API**: `GET /api/v1/complaints`
- **Location**: `internal/repository/complaint_repository.go` -> ฟังก์ชัน `GetPaginated`
- **Tables ที่เกี่ยวข้อง**: 
    - `module_complaints` (ตารางหลัก)
    - `department_module_module_types` (ใช้หา `AllowedModuleTypeIDs` ของกองนั้นๆ)
- **SQL Logic**:
```sql
WHERE 
    (module_complaints.department_id IN (?)) -- 1. งานที่รับมอบหมายในโหมด Central ปกติ
    OR 
    (
        module_complaints.department_id IS NULL AND -- 2. งานเก่าที่ไม่มีข้อมูลกอง (จากโหมด Direct)
        module_complaints.status != 'pending' AND  -- ต้องไม่ใช่เรื่องใหม่ (เรื่องใหม่ต้องไปที่ศูนย์กลาง)
        module_complaints.module_type_id IN (?)    -- และเป็นหมวดหมู่ที่กองนี้ดูแล
    )
```

### 2. การคัดกรองสิทธิ์ระดับหน่วยงาน (Strict Check)
ระบบจะ Join ตาราง Bridge เพื่อตรวจสอบว่าแอดมินมีสิทธิ์ใน **หมวดหมู่ย่อยนั้นๆ จริงหรือไม่** (อ้างอิงตามที่ติ๊กเลือกไว้ในหน้า Admin) แทนการดึงข้อมูลแบบเหมาทั้งมอดูล เพื่อความปลอดภัยของข้อมูล

---

## ระบบการให้คะแนนและการจัดการข้อโต้แย้ง (Rating & Dispute Flow)

ระบบรองรับการประเมินความพึงพอใจหลังจากจบงาน และกระบวนการ "ตีกลับงาน" เมื่อประชาชนไม่พอใจผลลัพธ์

### 1. การให้คะแนน (User Rating) และการบันทึกสถิติหน่วยงานแบบ Hybrid
- **เงื่อนไข**: ประชาชนจะสามารถให้คะแนนได้ต่อเมื่อเรื่องมีสถานะเป็น `completed` (เสร็จสิ้น) แล้วเท่านั้น
- **Logic การลงบันทึกประวัติ (Rating History)**:
    - บันทึกข้อมูลลงในตาราง `module_complaint_activities` (สถานะกิจกรรม: `user_rating`)
    - บันทึกคะแนนและสถานะลงในตาราง `module_complaint_rating_histories` เพื่อนำไปคำนวณคะแนนเฉลี่ยระดับบุคคลและระดับกองงาน
- **ตรรกะการจัดสรรคะแนนระดับหน่วยงาน (Hybrid Department Rating Routing)**:
    เพื่อรองรับการประเมินผลในสถานการณ์จริงที่พนักงานอาจทำงานข้ามกอง หรือช่วยงานแผนกอื่น ระบบใช้หลักการคัดกรองอัจฉริยะในฟังก์ชัน `GetCompleterInfo` ดังนี้:
    1. **โหมด Central (Option 2)**: ระบบจะบันทึกคะแนนสะสมรายกองให้กับ **"กองงานที่ได้รับมอบหมายเรื่องร้องเรียนตัวจริง"** (`complaint.department_id`) เช่น หากส่งเรื่องให้ *กองคลัง* แต่คนทำงานเสร็จอยู่ *กองช่าง* คะแนนระดับบุคคลจะไปที่คนทำงาน แต่คะแนนเฉลี่ยระดับกองจะถูกคิดให้ *กองคลัง* เพื่อให้ข้อมูลสอดคล้องกับ Dashboard และประเภทงานจริง
    2. **โหมด Direct (Option 1)**: เนื่องจากฟิลด์ `department_id` ในเรื่องเป็น `NULL` ระบบจะถอยกลับ (Fallback) ไปใช้ **"กองงานต้นสังกัดของตัวพนักงานผู้กดปิดงาน"** (`admin_departments`) เพื่อให้โหมด Direct ยังคงแสดงผลคะแนนและสถิติรายกองได้อย่างถูกต้องไร้รอยต่อ 100% โดยไม่ส่งผลกระทบใดๆ ทั้งสิ้น
- **ผลกระทบต่อสถิติและข้อมูลย้อนหลังเมื่อสลับโหมด (Impact on Historical Data when Switching Modes)**:
    * **ข้อมูลที่ถูกบันทึกไปแล้ว (Historical Rated Data)**: จะไม่มีการเปลี่ยนแปลงย้อนหลัง เนื่องจากระบบบันทึกคะแนนสะสมรายกองในลักษณะของ **Static History Snapshot** ณ เสี้ยววินาทีที่ถูกประเมินลงในตาราง `module_complaint_rating_histories` ซึ่งเป็นแนวทางมาตรฐานในการรักษาความถูกต้องและโปร่งใสของประวัติสถิติ (Audit Trail Integrity) ป้องกันสถิติในอดีตผันผวนจากการสลับโหมดการตั้งค่าเทศบาลในภายหลัง
    * **เรื่องใหม่และเรื่องรอดำเนินการ (Future/Active Data)**: เมื่อสลับโหมดการทำงานแล้ว ทุกเรื่องร้องเรียนที่ถูกปิดงานและประเมินผลหลังจากนั้น จะใช้เกณฑ์วิเคราะห์แบบ Hybrid ตามโหมดใหม่ทันที 100%

### 2. กระบวนการโต้แย้ง/ไม่พอใจผลงาน (Dispute / Dissatisfaction)
เมื่อประชาชนไม่พอใจผลลัพธ์และกดโต้แย้ง (Dispute) ระบบจะดำเนินการดังนี้:
- **การเปลี่ยนสถานะ**: สถานะของเรื่องจะถูกตีกลับจาก `completed` ไปเป็น `pending` (รอดำเนินการ) ทันที
- **การล้างข้อมูลผู้รับผิดชอบ**:
    - **AssigneeId**: จะถูกตั้งเป็น `NULL` เพื่อปลดพนักงานคนเดิมออก
    - **DepartmentId (โหมด Central)**: จะถูกตั้งเป็น `NULL` เพื่อส่งเรื่องกลับไปให้ศูนย์รับเรื่องฯ พิจารณาใหม่
    - **DepartmentId (โหมด Direct)**: ยังคงเดิม แต่เรื่องจะไปปรากฏเป็น "งานใหม่" ของกองนั้นๆ

### 3. การควบคุมการมองเห็นตามระดับสิทธิ์ (Role-Based Visibility)
เพื่อความโปร่งใสและป้องกันปัญหาเดิมซ้ำซ้อน ระบบจะ **จำกัดการมองเห็น** เรื่องที่ถูกโต้แย้ง ดังนี้:

| ระดับสิทธิ์ (Role Type) | การมองเห็นสถานะ `pending` / `received` |
| :--- | :--- |
| **Employees (พนักงาน)** | ซ่อน (จะเห็นเฉพาะงานที่มีชื่อตัวเองเป็น Assignee เท่านั้น) |
| **Managers (หัวหน้างาน)** | แสดง (เพื่อให้ตรวจสอบและมอบหมายงานให้พนักงานคนใหม่ได้) |
| **SuperAdmin / Center** | แสดง (เพื่อการบริหารจัดการภาพรวม) |

- **Technical Logic**: ในหน้าจัดการ (`complaint/manage`) ระบบจะเช็คค่า `AdminRoleType` หากเป็น `Employees` จะทำการกรองสถานะ `pending` และ `received` ออกจากผลลัพธ์โดยอัตโนมัติ เพื่อป้องกันไม่ให้พนักงานที่ "ทำงานพลาด" หรือ "โดนโต้แย้ง" เห็นเรื่องเดิมในกองงานกลางของตนเอง
