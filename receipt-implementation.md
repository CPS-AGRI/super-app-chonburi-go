# คู่มือและสัญญาระบบจัดจัดเก็บภาษีและใบเสร็จรับเงินอิเล็กทรอนิกส์ (อบจ. ชลบุรี)
## Electronic A4 PDF Receipt Generation & Payment Flow Specification

คู่มือฉบับนี้จัดทำขึ้นเพื่ออธิบายสถาปัตยกรรมระบบ รูปแบบการทำงาน และคู่มือการพัฒนาระบบจัดทำใบเสร็จรับเงินอิเล็กทรอนิกส์ (e-Receipt) ขนาด A4 และระบบงานชำระเงินประเภทต่างๆ ในโครงการ **Super App Chonburi**

---

## 📂 1. ภาพรวมของระบบงาน (System Overview)

ระบบใบเสร็จรับเงินอิเล็กทรอนิกส์ถูกพัฒนาขึ้นเพื่อให้บริการจัดทำ ดาวน์โหลด และจัดส่งใบเสร็จรับเงินรูปแบบทางการขนาด A4 ให้แก่ผู้ชำระภาษีและค่าธรรมเนียมของ อบจ. ชลบุรี (ภาษีโรงแรม, ภาษียาสูบ, และภาษีน้ำมัน/แก๊ส) โดยครอบคลุมทั้งช่องทางการโอนชำระเงินออนไลน์ และการเดินทางมาชำระเงินสดหรือเช็คด้วยตนเอง ณ หน้าเคาน์เตอร์ (Walk-in)

---

## ⚙️ 2. ตารางเปรียบเทียบสถานะธุรกรรม (Transaction Statuses)

ระบบทำการแบ่งแยกสถานะธุรกรรมและการยืนยันการชำระเงินออกเป็น 2 รูปแบบอย่างชัดเจน เพื่อประสิทธิภาพในการกระทบยอดและความปลอดภัยในการทำงาน:

| สถานะในระบบ | คำแสดงผลบนเว็บแอดมิน | รูปแบบและวิธีการตรวจสอบเงิน | ช่องทางการชำระเงิน |
| :--- | :--- | :--- | :--- |
| **`paid`** | **ชำระเงินแล้ว** | **ระบบตรวจสอบให้อัตโนมัติ (System Reconciled)**<br>ระบบ Reconciliation Engine ทำการประมวลผลจับคู่รหัสธุรกรรมทางการเงินโดยอัตโนมัติจากธนาคารกรุงไทย (KTB Text File รายวัน หรือ API Webhook ของ PromptPay QR Code) | โอนเงินออนไลน์ผ่านระบบแอปมือถือ / QR PromptPay Dynamic |
| **`verified`** | **ตรวจสอบผ่านแล้ว** | **เจ้าหน้าที่ตรวจสอบและอนุมัติ (Manual Verified)**<br>เจ้าหน้าที่กดยอมรับรายการหลังตรวจสอบเงินสดหรือเช็คสั่งจ่ายจริง ณ หน้างาน และทำการคีย์บันทึกข้อมูลหลักฐานเพิ่มเติมเข้าระบบด้วยตนเอง | ชำระเงินสดหรือเช็คสั่งจ่ายด้วยตนเอง ณ เคาน์เตอร์กองคลัง (Walk-in) |
| **`pending`** | **รอชำระเงิน** | อยู่ระหว่างรอหลักฐานทางการเงินหรือการกดอนุมัติ | - |
| **`audit_failed`** | **ตรวจสอบไม่ผ่าน** | เจ้าหน้าที่ตรวจสอบหลักฐานหรือยอดเงินแล้วพบว่าไม่ถูกต้อง | - |

---

## 🔄 3. ผังขั้นตอนการไหลของข้อมูล (System Flowcharts)

### A. ขั้นตอนการจ่ายเงินหน้าเคาน์เตอร์ (Walk-in Payment & Manual Audit Flow)
เมื่อผู้ชำระภาษีเดินทางมาจ่ายเงินด้วยตนเองที่หน้ากองคลัง อบจ. ชลบุรี:

```
[ผู้เสียภาษียื่นแบบผ่านมือถือสถานะ pending] ──► [เดินทางมาหน้าเคาน์เตอร์ ยื่นเลข Ref1/ชื่อร้าน พร้อมชำระเงินสด/เช็ค]
                                                                        │
                                                                        ▼
                                                [เจ้าหน้าที่เข้าเว็บแอดมิน /taxs ค้นหาร้านค้าและกดปุ่ม "ตรวจสอบ"]
                                                                        │
                                                                        ▼
                                                [ตรวจสอบเงินจริงเรียบร้อย กดปุ่มอนุมัติ "ตรวจสอบผ่านแล้ว"]
                                                                        │
                                                                        ▼
                                                [ระบบคีย์บันทึกเลขเช็ค/เงินสด และเปลี่ยนสถานะเป็น verified อัตโนมัติ]
                                                                        │
                                                ┌───────────────────────┴───────────────────────┐
                                                ▼                                               ▼
                                  [ระบบจัดส่งเมลแจ้งประชาชน]                       [เจ้าหน้าที่หน้าเว็บแอดมิน]
                                  ส่งไฟล์ใบเสร็จ A4 (PDF)                           กดพิมพ์ใบเสร็จกระดาษลงนาม
                                  แนบเข้าทางอีเมลอัตโนมัติ                            และส่งมอบให้ผู้รับทันที
```

### B. ขั้นตอนการกระทบยอดโอนเงินอัตโนมัติ (Online Reconciliation Flow)
เมื่อผู้ชำระภาษีจ่ายเงินผ่านแอปพลิเคชันมือถือ Chonburi Plus:

```
[ผู้ชำระเงินสแกนจ่าย QR Dynamic บนมือถือ] ──► [ยอดเงินเข้าบัญชี อบจ. ชลบุรี ธนาคารกรุงไทยโดยตรง]
                                                                  │
                                                                  ▼
                                            [ธนาคารกรุงไทยส่งรายงาน KTB .txt รายวันเข้าระบบหลังบ้าน]
                                                                  │
                                                                  ▼
                                            [Reconciliation Engine จับคู่จับกุมเลข Ref1/Ref2 อัตโนมัติ]
                                                                  │
                                                                  ▼
                                            [ระบบเปลี่ยนสถานะเป็น paid และประทับเวลาชำระเงินทันที]
                                                                  │
                                            ┌─────────────────────┴─────────────────────┐
                                            ▼                                           ▼
                              [ระบบจัดส่งเมลแจ้งประชาชน]                   [เจ้าหน้าที่หน้าเว็บแอดมิน]
                              ส่งไฟล์ใบเสร็จ A4 (PDF)                       ค้นหาตรวจสอบประวัติย้อนหลัง
                              แนบเข้าทางอีเมลอัตโนมัติ                        และกดดาวน์โหลดพิมพ์ใบเสร็จย้อนหลังได้
```

---

## 🛠️ 4. สถาปัตยกรรมตัวสร้างไฟล์ PDF (PDF Layout Engine Details)

เอนจิ้นสร้างใบเสร็จถูกพัฒนาอยู่ภายใต้แพ็คเกจ `pkg/pdf` ด้วยเทคโนโลยี Pure Go ผ่านไลบรารี `gofpdf` โดยมีรายละเอียดสำคัญดังต่อไปนี้:

### A. การจัดการฟอนต์ภาษาไทยและสระซ้อน (Thai Font & Vowel Alignment)
*   **ปัญหาหลัก**: ไลบรารี `gofpdf` ไม่มีตัวช่วยจัดการรูปอักขระภาษาไทย (Complex Text Layout - CTL) ส่งผลให้ฟอนต์สากลทั่วไปเกิดปัญหาสระบน วรรณยุกต์ และไม้เอก (`่`) วาดทับซ้อนกันจนมองเห็นเป็น `วันที` และ `เลขที`
*   **การแก้ไข**: ระบบถูกสลับมาใช้ฟอนต์ **TH Sarabun New Webfont** (พัฒนาโดย Phonbopit) ซึ่งปรับตั้งค่าตำแหน่งระยะความสูงและการจับคู่ glyphs ของสระภาษาไทยล่วงหน้าอย่างสมบูรณ์แบบ
*   **ที่ตั้งไฟล์ฟอนต์**:
    *   Regular Font: `assets/fonts/thsarabunnew.ttf`
    *   Bold Font: `assets/fonts/thsarabunnewbd.ttf`

### B. การจัดขนาดตัวอักษรและน้ำหนัก (Typography Design System)
*   **ขนาดหัวข้อเอกสาร (Title)**: `Sarabun Bold` ขนาด **`16pt`** เพื่อความสวยงามกะทัดรัดไม่เทอะทะ
*   **ข้อความชื่อ อบจ. ชลบุรี**: `Sarabun Bold` ขนาด **`11pt`** เพื่อความเป็นทางการและชัดเจนตามต้นฉบับ
*   **ข้อความข้อมูลผู้เสียภาษี (ได้รับเงินจาก)**: `Sarabun Regular` และ `Sarabun Bold` ขนาด **`10pt`** เพื่อความประณีตและป้องกันปัญหาชื่อร้านค้าขนาดยาวล้นเส้นขอบกระดาษ
*   **เลขที่ / วันที่**: `Sarabun Regular` ขนาด **`9pt`**
*   **ข้อมูลลายน้ำพื้นหลัง (Watermark)**: `Sarabun Regular` ขนาด **`15.0pt`** สีเทาอ่อนเรียบหรู (`230, 230, 230`) พิมพ์ 2 บรรทัดเอียงมุม 22 องศา

### C. การแบ่งรายละเอียดแถวข้อมูลชำระเงินของเช็ค (Cheque Split Layout)
ระบบมีกลไกตรวจจับคำว่า `"เลขที่"` ในหมายเหตุคลังอัตโนมัติ หากตรวจพบจะทำการแยกบรรทัดแสดงผลออกเป็น 2 บรรทัดในกล่องสรุปด้านล่างเพื่อความโปร่งใสและสวยงาม:
*   **บรรทัดที่ 1**: ชื่อธนาคารและสาขาของเช็ค (ขนานตรงแนวกึ่งกลางกับยอดตัวเลขเงินชำระทางขวามือที่พิกัด Y = `boxTop+10`)
*   **บรรทัดที่ 2**: เลขที่เช็คและวันที่สั่งจ่ายเงิน (พิกัด Y = `boxTop+16`)

---

## 🧪 5. การทดสอบและการรันระบบ (Verification & Testing)

ระบบมีชุดทดสอบจำลอง (Unit Test) ครอบคลุมการคำนวณแปลงบาทตัวอักษรภาษาไทย และการสร้างรูปเล่มไฟล์ PDF แยกประเภทภาษีครบทั้ง 3 รูปแบบ:

### คำสั่งสำหรับรันชุดทดสอบหลังบ้าน:
```powershell
go test ./pkg/pdf -v
```

### ผลลัพธ์จากการรันชุดทดสอบ (Generated Scratch Files):
หลังรันชุดทดสอบสำเร็จ ระบบจะบันทึกตัวอย่างใบเสร็จพร้อมข้อมูลสมมติที่สวยงามลงในโฟลเดอร์สำหรับตรวจรับงาน (UAT) ดังนี้:
1.  **ใบเสร็จภาษีโรงแรม (Hotel Fee Walk-In Cash)**: `scratch/test_hotel_receipt.pdf`
2.  **ใบเสร็จภาษียาสูบ (Tobacco Tax Cheque Payment)**: `scratch/test_tobacco_receipt.pdf`
3.  **ใบเสร็จภาษีน้ำมันและแก๊ส (Oil/Gas Tax Online Reconciled)**: `scratch/test_oil_gas_receipt.pdf`
